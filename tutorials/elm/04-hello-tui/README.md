# 04 — Hello TUI

Browser-based terminal UI: a monospace character grid with keyboard navigation.

## Run

```bash
bash run.sh
```

Then open `index.html` in a browser. Arrow keys move the cursor; printable keys write characters to the grid.
