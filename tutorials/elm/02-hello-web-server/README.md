# 02 - Hello Web Server

Minimal Elm app that fetches JSON from an API and decodes it.

## Run

```bash
bash run.sh
```

This runs the tests, compiles the app to `main.js`, then open `index.html` in a browser.
