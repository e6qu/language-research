# 03-hello-cli

Hello Command Palette -- a browser-based command interface built with Elm.

## Commands

- `help` -- list available commands
- `greet <name>` -- greet someone by name
- `echo <text>` -- echo text back

## Run

```
bash run.sh
```

Then open `index.html` in a browser.
