# 05 — Hello Concurrency

Parallel HTTP requests with `Cmd.batch` and a ticking `Time.every` subscription.

## Run

```bash
bash run.sh
```

This compiles the Elm app, runs the tests, and produces `main.js`. Open `index.html` in a browser to see the progress bar, tick counter, and fetched results.
