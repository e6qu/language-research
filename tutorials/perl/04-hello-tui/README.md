# 04-hello-tui

Terminal UI with ANSI rendering and raw input. State logic is testable.

## Run

```bash
make test    # run state logic tests
make e2e     # build verify only
perl bin/tui.pl  # interactive TUI
```
