# 04-hello-tui

Minimal terminal UI in Erlang. Navigate a list with arrow keys, quit with `q`.

## Run

```sh
chmod +x run.sh && ./run.sh
```

## Interactive demo

```sh
rebar3 eval 'hello_tui:start().'
```
