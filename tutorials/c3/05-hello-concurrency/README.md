# 05 — Hello Concurrency (C3)

Spawns multiple threads to simulate concurrent tasks, then joins them.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build
./build/hello-concurrency
```
