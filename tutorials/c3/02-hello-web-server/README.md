# 02 — Hello Web Server (C3)

Minimal TCP-based HTTP server returning a plain-text greeting.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build   # compile
make e2e     # start server, curl, stop
```
