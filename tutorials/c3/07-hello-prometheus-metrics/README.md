# 07 — Hello Prometheus Metrics (C3)

In-memory counter/gauge registry that renders Prometheus text exposition format.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build
./build/hello-metrics
```
