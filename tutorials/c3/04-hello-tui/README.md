# 04 — Hello TUI (C3)

ANSI terminal UI with a box-drawn greeting. Type text to redraw, or `quit` to exit.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build
./build/hello-tui
```
