# 08 — Hello OpenAPI Schema (C3)

Prints a static OpenAPI 3.0.3 JSON spec for a greeting API.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build
./build/hello-openapi | jq .
```
