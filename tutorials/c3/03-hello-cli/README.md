# 03 — Hello CLI (C3)

Command-line greeter with `--name` and `--shout` flags.

## Prerequisites

C3 is an experimental language. Install `c3c` from <https://c3-lang.org>.

## Usage

```bash
make build
./build/hello-cli --name C3 --shout
```
