# 03-hello-cli

CLI argument parser using plain Java. No external libraries.

## Usage

```bash
bash run.sh --name Alice --shout
```

## Flags

- `--name <name>` -- name to greet (default: World)
- `--shout` -- uppercase the output

## Test

```bash
make test
```
