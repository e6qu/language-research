# 04 - Hello TUI

Interactive terminal UI with ANSI escape codes. Navigate a selectable list using keyboard input.

## Run

```bash
bash run.sh
```

Keys: `j` down, `k` up, `Enter` select, `q` quit.

## Test

```bash
make test
```

## E2E

```bash
make e2e
```
