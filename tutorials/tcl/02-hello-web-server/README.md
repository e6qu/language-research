# 02 - Hello Web Server

Minimal HTTP server using Tcl's built-in `socket` command.

## Run

```bash
bash run.sh
```

Then visit `http://localhost:8080/` or `http://localhost:8080/greet/YourName`.

## Test

```bash
make test
```

## E2E

```bash
make e2e
```
