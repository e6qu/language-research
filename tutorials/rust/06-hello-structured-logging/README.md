# 06-hello-structured-logging

Structured JSON logging with `tracing` and `tracing-subscriber`.

## Run

```bash
cargo run
```

## Test

```bash
cargo test
```

Tests verify that log output is valid JSON with expected fields.
