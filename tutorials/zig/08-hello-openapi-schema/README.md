# 08-hello-openapi-schema

OpenAPI 3.0.3 spec as Zig structs, serialized to JSON via std.json.

## Build & Run

```bash
zig build
./zig-out/bin/hello-openapi-schema
```

## Test

```bash
zig build test
```
