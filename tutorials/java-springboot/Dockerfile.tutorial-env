FROM maven:3.9-eclipse-temurin-21-jammy AS base

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl jq python3 ca-certificates \
    && rm -rf /var/lib/apt/lists/*

RUN java --version && mvn --version | head -1

WORKDIR /tutorials
CMD ["bash"]
